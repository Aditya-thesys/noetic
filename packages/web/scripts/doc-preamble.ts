/**
 * Ambient preamble injected into bare documentation code fences by
 * `scripts/check-docs.ts`. It imports the full public surface of
 * `@noetic-tools/core` plus a handful of stand-in symbols the docs reference
 * narratively, so that snippets which omit imports still type-check.
 *
 * GENERATED from `packages/core/src/index.ts`. Regenerate with:
 *   bun scripts/gen-doc-preamble.ts
 * Do not edit the import lists by hand.
 */

export const DOC_PREAMBLE =
  "// Injected by check-docs.ts for bare snippets. Not part of the doc source.\nimport type {\n  AfterFirstTurnContext, AfterModelCallParams, AfterModelCallResult, AgentConfig, AgentHarnessContract, AgentHooks,\n  AskUserAnnotation, AskUserInput, AskUserOption, AskUserOutput, AskUserQuestion, BeforeToolCallParams,\n  BeforeToolCallResult, BranchRoute, BranchWorkflowNode, BudgetConfig, CallModelRequest, Channel,\n  ChannelHandle, CheckpointSnapshot, CheckpointStore, CompleteParams, Condition, ContentPart,\n  Context, ContextCacheConfig, ContextCacheStore, ContextConfig, ContextData, ContextEpoch,\n  ContextHarness, ContextLayer, ContextLayerHooks, ContextScope, ConvergeConfig, CreateCheckpointStoreOptions,\n  CreateNudgeMessageOpts, CwdSnapshot, CwdState, DeliveryMode, DetachedHandle, DetachedSignal,\n  DeveloperMessageExtensionItem, DisposeParams, DurableTaskState, DynamicWorkflowOpts, EmbedFn, EpochUsage,\n  EveryOptions, EveryWorkflowNode, ExecuteInput, ExecuteOptions, ExecuteStepFn, ExecutionContext,\n  ExecutionOutcome, ExtendedItem, ExtensionItem, ExternalChannel, FactExtractor, FactSearcher,\n  FileSearchItem, ForkWorkflowNode, FrameworkStreamEvent, FrontierFrame, FsAdapter, FsStats,\n  FunctionCallItem, FunctionCallOutputItem, HarnessResponse, HarnessStatus, HistoryWindowConfig, HydrationContext,\n  ImageGenerationItem, InferContext, InferContextShape, InferExtendedItem, InitParams, InitResult,\n  InputContentPart, InputFilePart, InputImagePart, InputMessageItem, InputTextPart, InterviewOpts,\n  InterviewQuestionAnswer, InterviewResult, Item, ItemBase, ItemLog, ItemLogSnapshot,\n  ItemSchemaExtensions, LLMResponse, LastLayerUsage, LayerCallModelRequest, LayerDataDecl, LayerFunctionDecl,\n  LayerPlacement, LayerProvides, LayerTimeouts, LayerTraceSpan, LayerUsageEntry, LedgerEntry,\n  LlmProviderConfig, LlmWorkflowNode, LoopConfig, LoopWorkflowNode, MergeStrategy, MessageItem,\n  ModelParams, NoeticError, ObservationalContextConfig, ObservationalState, OtherwiseClause, OutputItem,\n  OutputTextPart, ParseAndRunWorkflowOpts, PendingAskUserSnapshot, PlanConstraints, PlanContextConfig, PlanEnterSessionCallback,\n  PlanExecutionEntry, PlanExitCallback, PlanNode, PlanState, ProcessSubprocessRequest, ProjectionPolicy,\n  ProvideWorkflowNode, ReanchorReason, ReasoningItem, ReasoningTextPart, RecallLayerOutput, RecallParams,\n  RecallResult, RefusalPart, RenderDeltaParams, RestoreContextOptions, RetryPolicy, ReturnParams,\n  ReturnResult, RoundUsage, RunnableLoopHarness, RunnableLoopOpts, ScopedStorage, SdkStreamEvent,\n  SequenceWorkflowNode, SerializedError, ServerToolItem, ServerToolSpec, SessionScope, SessionSeedHarness,\n  SettleResult, ShellAdapter, ShellExecOptions, ShellExecResult, Snapshot, Span,\n  SpawnParams, SpawnResult, SpawnWorkflowNode, StallNudgeOpts, SteeringConfig, SteeringDecision,\n  SteeringRule, SteeringState, Step, StepBranch, StepFork, StepForkAll,\n  StepForkRace, StepForkSettle, StepLLM, StepLedgerRetention, StepLedgerStats, StepLoop,\n  StepMeta, StepProvide, StepRun, StepSpawn, StepSubHarness, StepSubprocessOverrides,\n  StepSubprocessRequest, StepTool, StepWorkflowOpts, StorageAdapter, StoreParams, StoreResult,\n  StreamEvent, StreamingItem, SubHarness, SubHarnessBuiltinTool, SubHarnessContinueState, SubHarnessContinueTurnOptions,\n  SubHarnessFinishReason, SubHarnessPromptTurnOptions, SubHarnessResumeState, SubHarnessRunContext, SubHarnessSession, SubHarnessSessionPolicy,\n  SubHarnessSettings, SubHarnessStartOptions, SubHarnessStreamPart, SubHarnessTurnResult, SubHarnessWorkflowNode, SubflowWorkflowNode,\n  SubprocessAdapter, SubprocessControlResult, SubprocessHandle, SubprocessHandleMetadata, SubprocessRequest, SubprocessStatus,\n  SubprocessStopResult, SummaryTextPart, TemporalContextConfig, TemporalFact, TemporalSearchResult, TokenUsage,\n  Tool, ToolContext, ToolContextDeclaration, ToolExecutionContext, ToolWorkflowNode, TraceExporter,\n  Until, UntilPredicate, Verdict, VerifyFn, WebSearchItem, WhenClause,\n  WorkflowDocument, WorkflowNode, WorkingMemoryContextConfig, WorkingMemoryContextState,\n} from '@noetic-tools/core';\nimport {\n  AgentHarness, AskUserAnnotationSchema, AskUserInputSchema, AskUserOptionSchema, AskUserOutputSchema, AskUserQuestionSchema,\n  CheckpointKeys, CheckpointSchemaVersion, CheckpointSnapshotSchema, CwdSnapshotSchema, DEFAULT_NUDGE_MESSAGE_TEXT, DEFAULT_STEP_LEDGER_RETENTION,\n  DetachedStatus, FrontierFrameSchema, GenAI, InMemoryExporter, ItemLogSnapshotSchema, ItemSchema,\n  ItemSchemaRegistry, LedgerEntryKind, MergeStrategySchema, NoeticAttr, NoeticConfigError, NoeticErrorImpl,\n  NoopExporter, PendingAskUserSnapshotSchema, PlanNodeSchema, PlanPhase, PlanStyle, SUB_HARNESS_KINDS,\n  Slot, SteeringAction, SubHarnessKind, SubHarnessStreamPartSchema, TIMEOUT_ERROR_PREFIX, ToolAttr,\n  UntilPredicateSchema, WorkflowDocumentSchema, WorkflowNodeSchema, adaptivePlan, aiCondition, all,\n  allCondition, any, anyCondition, branch, channel, compilePlan,\n  context, cosineSimilarity, createCheckpointStore, createDetachedSignal, createInMemoryFsAdapter, createInMemoryShellAdapter,\n  createInMemoryStorage, createInMemorySubprocessAdapter, createMessage, createNudgeMessage, createOpenRouterEmbed, createStallNudgeHook,\n  defaultItemSchemaRegistry, durableTaskState, dynamicWorkflow, embeddingMatch, estimateTokens, every,\n  execute, fileReference, findFunctionCall, fork, getRegistry, getToolCwd,\n  historyWindow, hydrateNode, hydrateWorkflow, interview, isNoeticConfigError, isNoeticError,\n  isServerToolSpec, layerData, layerFn, lookupStep, loop, observationalContext,\n  otherwise, parseAndRunWorkflow, planContext, provide, ralphWiggum, react,\n  registerStep, runnableLoop, seedFromItems, semanticRoute, semanticSwitch, serializeError,\n  setToolCwd, snapshotCwdState, spawn, staticContent, steering, step,\n  storageGetMany, stripUnresolvedToolCalls, temporalContext, tool, toolContextLayer, toolWithGenerator,\n  until, validateWorkflow, walkWorkflow, when, workflowDepth, workingMemoryContext,\n} from '@noetic-tools/core';\n\ndeclare const searchTool: Tool;\ndeclare const calcTool: Tool;\ndeclare const agent: Step<ContextData, string, string>;\ndeclare const observer: (buffer: ReadonlyArray<unknown>) => Promise<string[]>;\ndeclare const semanticRecall: ContextLayer;\n";
